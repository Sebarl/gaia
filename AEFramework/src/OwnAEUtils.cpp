#include "OwnAEUtils.h"
#include <math.h>
/*
long round(double x){
    double fractionalPart = fabs(modf(x, NULL));
    if (fractionalPart >= 0.5)
		return x>=0?ceil(x):floor(x);
	else
		return x>=0?floor(x):ceil(x);
}*/
